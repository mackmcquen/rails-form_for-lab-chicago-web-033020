<h1> Students Index </h1>

<%= button_to "Add New Student", new_student_path, method: :get %>
<ul>
    <% @students.each do |student| %>
        <li><%= link_to student.first_name, student_path(student) %> <%= link_to student.last_name, student_path(student) %></li>
    <% end %>
</ul>
